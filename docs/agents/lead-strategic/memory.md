# Lead-Strategic Memory

## Active Strategic State

- active wave:
  - `Agent Workflow Simplification Wave 1`
- current plan:
  - `docs/agents/lead-strategic/current_plan.md`
- active branch:
  - `feature/agent-model-runtime-validation`
- operating mode:
  - `high-risk iterative / unstable wave`
- immediate next slice:
  - `ST-0: Memory Policy And Prune Pass`

## Still-Valid Durable Decisions

- Preserve these ownership rules through the simplification wave:
  - `lead-strategic` owns planning and acceptance
  - `orchestrator` owns execution flow, not product-code implementation
  - `direct-fix` remains narrow
  - truthful evidence and minimum independent review floor must survive wording changes
- `mixed-claude-workers` remains the practical default for ordinary waves.
- The runtime-validation wave for `opus-orchestrated-codex-workers` is closed.
  Consult archived plan/report artifacts only if a future task explicitly targets that profile again.

## Memory Problem To Resolve In This Wave

- Current durable memory tends to accumulate closed-wave detail that belongs in reports or archive artifacts.
- This wave must decide whether durable memory stays as a tiny current-state bootstrap or leaves the default bootstrap path.
- Until that decision is made, treat this file as a minimal resume snapshot, not a narrative log.

## Resume Point

- Start with `ST-0`.
- Decide `keep-minimal durable memory` vs `remove memory from default bootstrap`.
- After that, freeze the migration map and continue the docs simplification slices.
