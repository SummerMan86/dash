/**
 * ECharts Presets (compat re-export)
 *
 * NOTE: Presets are part of shared UI/design system; this file remains to keep
 * `$entities/charts` imports working.
 */

export * from '$shared/ui/chart/presets';
