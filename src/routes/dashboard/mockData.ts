/**
 * Compatibility re-export.
 *
 * Demo pages used to import mocks from `routes/dashboard/mockData.ts`.
 * The server layer must NOT import from `routes/*`, so fixtures live in `shared/`.
 */

export * from '$shared/fixtures/paymentAnalytics';
