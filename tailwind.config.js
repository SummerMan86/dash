/** @type {import('tailwindcss').Config} */
export default {
    content: [
      './src/**/*.{html,js,svelte,ts}',
    ],
    // Tailwind 4: большинство конфигурации в CSS через @theme
    // Здесь только content paths и опциональные плагины
}