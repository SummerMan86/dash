/**
 * Compatibility re-export.
 *
 * Demo pages used to import mocks from `routes/dashboard/mockData.ts`.
 * The server layer must NOT import from `routes/*`, so fixtures live in `src/lib/fixtures/`.
 */

export * from '$lib/fixtures/paymentAnalytics';
